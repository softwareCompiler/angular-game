import {CircleDirective} from './circle.directive';

// Why the unused spec files are not deleted?
describe('CircleDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleDirective();
    expect(directive).toBeTruthy();
  });
});
