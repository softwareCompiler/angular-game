import { SquareContainerDirective } from './square-container.directive';

describe('SquareContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new SquareContainerDirective();
    expect(directive).toBeTruthy();
  });
});
