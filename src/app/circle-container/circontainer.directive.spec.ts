import { CircontainerDirective } from './circontainer.directive';

describe('CircontainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CircontainerDirective();
    expect(directive).toBeTruthy();
  });
});
